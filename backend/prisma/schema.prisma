generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  uuid         String   @id
  prefectureId Int
  steps        Int      @default(0)
  updatedAt    DateTime @updatedAt

  @@index([prefectureId])
}
