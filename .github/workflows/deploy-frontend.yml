name: Deploy Frontend to Object Storage

on:
  push:
    branches:
      - deploy
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-frontend.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build
        working-directory: frontend
        env:
          VITE_API_URL: ${{ secrets.BACKEND_URL }}
        run: npm run build

      - name: Configure AWS CLI for Sakura Object Storage
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.S3_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.S3_SECRET_ACCESS_KEY }}
          aws-region: jp-north-1

      - name: Deploy to Object Storage
        working-directory: frontend
        run: |
          aws s3 sync dist/ s3://${{ secrets.S3_BUCKET_NAME }}/ \
            --endpoint-url https://${{ secrets.S3_ENDPOINT }} \
            --delete \
            --cache-control "public, max-age=31536000, immutable" \
            --exclude "index.html"

          aws s3 cp dist/index.html s3://${{ secrets.S3_BUCKET_NAME }}/index.html \
            --endpoint-url https://${{ secrets.S3_ENDPOINT }} \
            --cache-control "public, max-age=0, must-revalidate"

      - name: Deploy notification
        run: echo "Frontend deployed successfully to https://${{ secrets.S3_BUCKET_NAME }}.s3-website-jp-north-1.sakurastorage.jp"
